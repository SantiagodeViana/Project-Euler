#include <stdio.h>
#include <stdlib.h>

int primeProof(int n);}

//p^3 q^3, p && q primer = isSqube

int main()
{

    printf("Hello world!\n");
    return 0;
}

int primeProof(int n){ //No es necesario implementar porque sabemos que f(200) = 1
    int primeProof;
    while (i < digCount && primeProof == 1){
        while (j < 10 && primeProof == 1){
            if (isPrime(aux)) primeProof = 0;
        }
    }
    return primeProof;
}
