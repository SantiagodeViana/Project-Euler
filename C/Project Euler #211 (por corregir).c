#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int divSquareSum(int n);

/* Solución para el problema #211 de Project Euler
 * "¿Cuántos números de Hamming generalizados de tipo 100 hay que no excedan 10⁹?"
 * https://projecteuler.net/problem=211       */

int main()
{
    int aux, respuesta = 0;
    for (int i=0; i < 64000000; i++){
        aux = divSquareSum(i);
        if (sqrt.(aux) == (int)sqrt.(aux)) respuesta++;
    }
    return 0;
}

int divSquareSum(int n){
    int sum;
    while(divSquareSum => 0){
        sum *= div*div;
        n /= 10;
    }
    return sum;
}
